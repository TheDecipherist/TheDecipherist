## Project Snapshot
Generated: 2026-04-22 | Branch: main

## Stack
Framework: None (pure static HTML/CSS/JS) | DB: None | Host: GitHub Pages (docs/ folder)

## Features Documented
- [01-create-github-pages](docs/01-create-github-pages.md) — TheDecipherist GitHub Pages Site — **complete**

## Last Audit
(no audit run yet — run /mdd audit to generate findings)

## Rules Summary
Read PROMPT.md for the full spec. Key rules:
- OKLCH-only colors — no hex, no rgb, no hsl in CSS source
- BEM naming convention throughout
- CSS @layer structure: reset, tokens, base, layout, components, animations, utilities
- Pure static — zero frameworks, zero utility classes
- Self-hosted fonts (woff2, Latin subset) — clash-display + jetbrains-mono
- Critical CSS inlined in every <head>
- Semantic HTML — zero <div> where semantic elements exist

---

## Notes
- Font files (docs/fonts/*.woff2) still need to be obtained — site falls back to system fonts until then
- Icon files (docs/icons/*.png) still needed for full PWA manifest support
- Site verifies at: bash scripts/verify.sh (37/37 passing)
- CSS built via: bash scripts/build-css.sh → docs/css/main.css (37.3KB)
